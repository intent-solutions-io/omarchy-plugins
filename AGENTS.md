# Agent Instructions

Follow `CLAUDE.md`, `CONTRIBUTING.md`, and `SECURITY.md`. Preserve existing user
changes and generated-file ownership rules.

Never hand-edit generated marketplace data or plugin detail pages. Keep
omaTrail capitalized exactly this way. Do not add em dashes or en dashes.

Before pushing, run `bash scripts/check-site.sh`, validate changed workflow
files, inspect the full diff, and include screenshots for visible changes.
